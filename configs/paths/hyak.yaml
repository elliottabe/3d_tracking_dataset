user: ${oc.env:USER,eabe}

# set the directory where the output files get saved
cwd_dir:  /gscratch/portia/${paths.user}/Research/MyRepos/3d_tracking_dataset/
base_dir: /gscratch/portia/${paths.user}/data/Johnson_lab/${dataset.name}/${version}
save_dir: ${multirun_save_dir:${paths.base_dir},${run_id}}
log_dir:  ${paths.save_dir}/logs/
ckpt_dir: ${paths.save_dir}/ckpt/
fig_dir:  ${paths.save_dir}/figures/
data_dir: /gscratch/portia/${paths.user}/data/Johnson_lab/${dataset.name}/${version}

body_model_dir: /gscratch/portia/${paths.user}/Research/MyRepos/fruitfly_body_models/
project_dir: /gscratch/portia/${paths.user}/Research/MyRepos/

# courtship pipeline path keys (Task 1)
data_dir_johnson: /gscratch/portia/${paths.user}/data/Johnson_lab
out_root: ${paths.data_dir_johnson}/courtship
vit_runs_root: ${paths.data_dir_johnson}/jax_vitpose_runs

# curated real-annotation V3 dataset root (Task 7: mixed into detector finetune)
red_data_v3_root: ${paths.data_dir_johnson}/red_data/red_data_unified_V3