# SDD ledger — plan: docs/plans/2026-09-03-mvq-dinov3-query-decoder-p0-p2.md
Spec: docs/specs/2026-09-03-mvq-dinov3-query-decoder-design.md (reachable, binding authority)
Worktree: .claude/worktrees/mvq-p0-p2, branch mvq-p0-p2, base ea087c0

## Pre-flight conflict scan (2026-09-03)
| pair / task | produces vs consumes | finding |
|---|---|---|
| T1 -> T5 | `DINOv3(cfg, rngs)`, `__call__(x, *, remat)`, `DINOv3Config.vitb16()/vitl16()`; T5 does `dataclasses.replace(base, embed_dim, depth, num_heads)` | consistent |
| T1 -> T7 | `load_dinov3_safetensors(model, dir)`, `dinov3_snapshot(repo)`, `HF_REPOS` | consistent |
| T1 <-> backbone.py | factory `(cfg, *, rngs)`; test expects (1,16,32) tokens at 64px | consistent |
| T2 -> T3 | T3 uses its own numpy `_affine_np` instead of T2's jnp `affine_rows` | 5-line duplication, deliberate (no JAX in loader threads). Ruling: accept; reviewer may flag as Minor |
| T2 -> T4 | `warp_cameras`, `rotate_world(M,R)`, `mirror_world(M,t,crop)` | T4 imports warp_cameras but writes the einsum inline -> unused import, Minor |
| T2 -> T5/T6 | `project_local(X(...,3), M(C,2,3), t(C,2)) -> (...,C,2)`; vmapped over B,T then moveaxis | consistent |
| T3 -> T4/T6/T7 | WINDOW_KEYS; loss needs px_scale (B,), fly_valid (B,F), cam_valid, prompt_on (added by trainer) | consistent |
| T3 internal | census test 4 / 2 windows; T=2 two-fly test | consistent with fixture; fixture fly1 offset (40,5,0) may project outside 448-px height in rotated cams -> only needs ANY visible view; implementer may shrink offset to ~25 units if fly_valid[1] fails |
| T5 internal | `test_prompt_changes_only_instance_zero_when_on` asserts d1 < d0 at fresh init | plausible, not guaranteed. Ruling: if it fails at init, relax to `d0 > 1e-6` and `d1 <= d0` and report |
| T5 internal | invalid-camera invariance: tokens zeroed pre-fusion, key masks, refine gather weighted by cam_valid | consistent |
| T6 internal | `test_repulsion_fires_only_near_other_fly_same_part` uses part index [0,9,2,3,4] to make rep vanish — WRONG: part ids are per keypoint index, shared by both flies, so kp1 vs other kp1 is always same-part | Ruling: amend test — move fly0 kp1 onto other fly's kp2 (different part) for the zero case, onto kp1 for the positive case, both under arange parts |
| T6 internal | `test_masked_entries_do_not_contribute` corrupts kp0 by +100 units; matching margin gets thin | Ruling: corrupt by +30.0 units instead (A/B cost analysis: 426 vs ~1112) |
| T6 internal | perfect-batch total < 0.05 (only -log c and BCE floors) | consistent (~0.003) |
| T7 internal | smoke test cohorts female/two_fly/group_A present in fixture; ckpt_dir=None path; window_lengths (1,2) with 2 T=2 windows at batch 2 | consistent |
| T7 -> Hydra | model yaml has `arch: mvq`, dropped by build_dataclass | consistent |
| T1 Step 5 | `pip install transformers` into the shared 3d_tracking conda env | side effect outside worktree -> ask the user (asked at setup) |
| Global | GPU work only via scripts/slurm/submit_task.sh (node busy) | all GPU steps in T1/T8 use the queue |

## Model selection
implementers: sonnet (plan carries full code; JAX subtleties justify mid-tier); task reviewers: sonnet (T1-T4, T7-T8), opus (T5, T6); final review: fable.

## Progress
Setup: baseline subset 23 passed / 1 skipped (42s, CPU). User consented 2026-09-03 to `pip install transformers` in the 3d_tracking env for the T1 parity test.
Task 1: dispatched (BASE ea087c0, implementer sonnet)
Task 1: implementer DONE_WITH_CONCERNS (e7d5ddd): safetensors 0.7.0->0.8.0 bumped in shared env (beyond consent; jax/flax/torch/numpy untouched); transformers resolved to 5.16.1 (state_dict 'model.' prefix stripped in loader); circular import in backbone.py registration fixed. Review dispatched (sonnet).
Task 1: review — spec ✅; Important #1 = env mutation (safetensors 0.7.0->0.8.0 collateral to the user-approved transformers install), not a code defect. Ruling: KEEP safetensors 0.8.0 — minor, backward-compatible release; reverting would break the parity test the user asked for; jax/flax/torch/numpy verified untouched; user informed 2026-09-03 — cost if wrong: a SAM3/HF load path regresses on 0.8 and needs a pin back to 0.7.0 (one pip command).
Task 1: minor (deferred): no test for the "model param not filled -> RuntimeError" loader branch (verified manually by reviewer)
Task 1: minor (deferred): dead split/join round-trip in the keystr branch of load_dinov3_safetensors
Task 1: minor (deferred): unconsumed vs not-filled error messages use different separators
Task 1: minor (deferred): `model.` prefix strip in the loader exists only for the transformers-5.x test path
Task 1: complete (commits ea087c0..e7d5ddd, review clean after ruling; 4 minors deferred)
Task 2: dispatched (BASE e7d5ddd, implementer sonnet)
Task 2: implementer DONE (b20e497), 7/7. Review dispatched (sonnet).
Task 2: review ✅ Approved. ⚠️ token-centre order vs backbone flatten: resolved by controller — dinov3.py reshapes (B,h,w,D)->(B,h*w,D), row-major, matches. ⚠️ trailer: verified via git log (present).
Task 2: minor (deferred): affine_rows host-sync bool() deserves an in-source comment; inconsistent type hints
Task 2: complete (commits e7d5ddd..b20e497, review clean)
Task 3: dispatched (BASE b20e497, implementer sonnet)
Task 3: implementer DONE_WITH_CONCERNS (eb7bd5f), 14/14. Census: T=1 2661/153 exact; T=2 835/22 (plan estimate 735/15 came from the survey's per-recording pair count; implementer recounted independently — accepted); two-fly windows 88/74 (= 2 windows per two-fly frameset, one per host; fly_id==1 framesets 44/37 exact). Fixture `%YAML` format-string fix accepted.
Task 3: Ruling: `tests/__init__.py` breaks collection of 3 pre-existing tests that use bare sibling imports — REMOVE it and import the fixture bare (`from mvq_fixtures import ...`), matching the repo's existing test convention; Tasks 4 and 7 briefs will be told the same — cost if wrong: none beyond a later import-style cleanup.
Task 3: follow-up 8d10195 applied the ruling; 16 passed/2 skipped incl. the 3 previously-broken tests. Review dispatched (sonnet) over b20e497..8d10195.
Task 3: review — spec ✅; Important: shared numpy Generator mutated from window_batches' thread pool (jitter draws unsynchronised). Ruling on the fix shape: per-sample Generator from SeedSequence([seed, idx, epoch]) with window_batches setting ds.epoch = its seed — thread-safe and reproducible without locks.
Task 3: minor (deferred): M/t derived from float32 camera_matrices while DLT uses float64 (~1e-2 px systematic) — consider the float64 stack
Task 3: minor (deferred, plan-mandated): host with no visible 3D joint silently centres at the origin (0/2814 real windows hit it)
Task 3: minor (deferred): hardcoded (1936,448) fallback; per-fly sex overwritten per frameset (last wins); untested calib_group/recordings filter/_affine_np raise; bare KeyError for a recording missing from the manifest
Task 3: fix round 1/5 dispatched (resume implementer)
Task 3: fix round 1/5 — implementer commit 833a13e (per-sample SeedSequence RNG + test); scoped re-review dispatched (sonnet) over 9e41bff..833a13e
Task 3: fix round 1/5 (1 addressed, 0 open — thread-safe per-sample jitter RNG; commits 9e41bff..833a13e)
Task 3: complete (commits b20e497..833a13e, review clean after 1 fix round; 4 minors deferred)
Task 4: dispatched (BASE 833a13e, implementer sonnet)
Task 4: implementer DONE (cb0e962), 7/7 (+8 loader tests), ~27s. Note: camera dropout keyed on frame-0 cam_valid (window origin shared, cams present per window) — acceptable. Review dispatched (sonnet).
Task 4: review ✅ Approved. ❌-labelled deviation (affine_warp_image/warp_cameras not called) — Ruling: accepted; the controller's dispatch explicitly allowed the inline einsums and the self-contained `_warp_rgb`, and the painted-pixel test proves the convention — cost if wrong: none (duplication of ~10 lines).
Task 4: minor (deferred, LOAD-BEARING for P3 T=2 training): `_camera_dropout` keys the >=3-valid floor on frame-0 cam_valid only; for T>1 windows a frame with fewer valid cams can drop below 3. Fix in the final-review fix wave or before any T=2 run: reference = cam_valid.all(axis=1), floor on that count.
Task 4: minor (deferred): MVAugParams not frozen (unhashable); no composed-all-ops consistency test; meshgrid rebuilt per vmapped call; `kp` name shadowing
Task 4: complete (commits 833a13e..cb0e962, review clean)
Task 5: dispatched (BASE cb0e962, implementer sonnet, reviewer will be opus)
Task 5: implementer DONE (554de13), 19 passed. Deviation: xyz head kernel default-init instead of zeros (zero kernel+bias made xyz identically 0 -> prompt test deterministically failed). Interface-prose vs code mismatches noted (geom_cam internal, _bank, assemble as free fn). Review dispatched (opus).
Task 5: review (opus) — spec mostly ✅; Important: (1) xyz head default init => final xyz == 2x pass-1 at step 0 (measured); (2) decoder attention materialises (B,heads,2100,10976) logits, ~1.1 GB/sample/layer at the shipped config, plan-mandated, uncosted; (3) assemble lacks the "no valid view -> NaN" half of spec 4.6, plan-mandated.
Task 5: Ruling on (2): fix NOW (load-bearing for Task 7 training): chunk the query axis in masked_attention with lax.map (static chunk, default 512) and wrap decoder blocks in nnx.remat; test chunked == unchunked — cost if wrong: slower decoder, no accuracy impact.
Task 5: Ruling on (3): add optional cam_valid (B,T,C) to assemble; a frame with zero valid cameras -> NaN/conf 0 for all its keypoints; per-keypoint visibility gating belongs to the P4 lifter (uses vis_logit) — cost if wrong: P4 revisits assemble.
Task 5: minor (deferred, LOAD-BEARING for P3 scale-up): backbone="dinov3_l16" is a silent no-op (MVQConfig overrides embed_dim/depth/heads) — fix wave
Task 5: minor (deferred): no final LayerNorm before heads (plan-mandated); patch not plumbed to DINOv3Config; no T<=max_frames / divisibility validation; unused imports; prompt threshold >0.5 can yield a constant prompt token; aux_pass1 semantics for refine_passes>1; toks*cam_valid not NaN-safe; refine gather assumes square crops; Interfaces-prose vs code drift (3-tuple decoder return, _bank, free assemble)
Task 5: fix round 1/5 dispatched (resume implementer) — findings 1, 2, 3 + TINY dec_layers_3d=4 so aux_layers is non-empty in tests
Task 5: fix round 1/5 — implementer commit 17d0625; aux_layers length is 2 (one intermediate readout per pass x 2 passes) — controller's '== 1' was wrong, 2 is correct; scoped re-review dispatched (sonnet)
Task 5: fix round 1/5 (3 addressed + minor, 0 open — small-scale xyz init, chunked+remat decoder attention, cam_valid NaN policy; commits 554de13..17d0625). Re-reviewer confirmed finite non-zero grads through the remat path.
Task 5: minor (deferred): no committed gradient-through-remat test (pre-existing convention gap shared with dinov3.py)
Task 5: complete (commits cb0e962..17d0625, review clean after 1 fix round; minors deferred)
Task 6: dispatched (BASE 17d0625, implementer sonnet, reviewer will be opus)
Task 6: first implementer terminated by a session rate limit (HTTP 429, reset 17:50 PDT) before writing anything — worktree clean, no report. Re-dispatched 17:52 (BASE 17d0625, fresh sonnet).
Task 6: implementer DONE (bf6106f), 15 passed; fixed a bracket SyntaxError in the brief's _perfect_batch; rep normalisation kept verbatim (sum over other-keypoint axis). Review dispatched (opus).
Task 6: review (opus) — Critical: px_scale broadcast in the matching cost lands on the fly axis (crashes for B != F; silently wrong at B == F) — plan-mandated bug in the brief's code, invisible because every test uses B=F=2. Important: (1) visibility BCE mask is (B,F,T,C,1) so _mmean's denominator is K× too small — vis enters at ~3.2 effective weight instead of 0.1; (2) no test for the single-fly path or the aux_layers path.
Task 6: Ruling (minor 1 → fix now): normalise the repulsion hinge by same-part cardinality (same_part / max(same_part.sum(1),1)) so single-member and six-member parts carry equal nominal weight — cost if wrong: repulsion ~6x weaker on tarsal tips than the verbatim spec code; weight is swept in P3 anyway.
Task 6: Ruling (minor 6 → fix now): confidence term via log_sigmoid (no clip) so a saturated head keeps gradient — cost if wrong: none expected.
Task 6: minor (deferred): conf term has no outer weight (0.2 is λ) — document in-source; duplicate reprojection for match_reproj_px; unused locals/imports; pin when fly 0 invalid (unreachable today)
Task 6: fix round 1/5 dispatched (resume implementer)
Task 6: fix round 1/5 — implementer commit 6156619 (px_scale axis, _mmean broadcast, 3 tests, per-part repulsion norm, log_sigmoid conf); scoped re-review dispatched (sonnet)
Task 6: fix round 1/5 (Critical + 2 Important + 2 rulings addressed, 0 open; commits bf6106f..6156619)
Task 6: complete (commits 17d0625..6156619, review clean after 1 fix round; minors deferred)
Task 7: dispatched (BASE 6156619, implementer sonnet, reviewer will be opus)
Task 7: implementer DONE (ed86048), 27/27 incl. smoke (50s CPU); cohort check moved before model build; val_cohorts default (female, two_fly). Review dispatched (opus).
Task 7: review (opus) — Critical: _with_ema aliases the live model's Variables (nnx.merge(*nnx.split(m)) shares them), so every eval overwrites training weights with the EMA (verified empirically). Important: per-group instead of global grad clip (plan-mandated); EMA not checkpointed -> lost on resume (plan-mandated); paths.mvq_runs_root never applied (run_dir_for uses runs_root); evaluate re-decodes JPEGs single-threaded twice per eval; uv2d_px / head-vs-reproj metric missing; rigid_spread labels can desync from filtered seg_idx.
Task 7: Rulings: (a) nnx.clone before nnx.update + a test asserting raw params unchanged after evaluate; (b) global clip = chain(clip_by_global_norm, multi_transform(adamw groups)); (c) EMA checkpointed via an mvq-local CheckpointManager with item_names (model, opt, ema) — shared checkpoint.py untouched; (d) runs root: keep run_dir_for(cfg) (so .hydra/ sits beside the run, which P4 relies on) and require the launch override `paths.runs_root=${paths.mvq_runs_root}` — main() warns loudly when runs_root != mvq_runs_root; add mvq_runs_root to workstation.yaml too; (e) evaluate uses window_batches(shuffle=False, drop_last=False, num_workers) and computes prompted+unprompted per decoded batch; (f) add uv2d_px and head_vs_reproj_px metrics to mvq_loss (touches losses_mvq.py + its test) and report them; (g) exist_prec/rec restricted to two-fly windows per spec §7. Cost if wrong: (c) adds ~1 GB per checkpoint step for the EMA copy; (d) a launch without the override lands in jax_cached3d_runs with a warning.
Task 7: minor (deferred): duplicated _labels/_balanced_weights formulas; eval unsharded on device 0 + retrace on the partial batch; full mvq_loss run at eval; prefetch threads never shut down; start==total_steps -> nan final_loss; NaN in JSON
Task 7: fix round 1/5 dispatched (resume implementer)
Task 7: fix round 1/5 — implementer commit d49c9da (8 findings + cheap fixes); scoped re-review dispatched (sonnet)
Task 8 prep (controller): submit_task.sh wires --gpus=$GPUS and --cpus-per-task=$CPUS (no copy needed); it sets XLA_PYTHON_CLIENT_MEM_FRACTION=0.6 by default -> training commands must prefix `export XLA_PYTHON_CLIENT_MEM_FRACTION=0.9 &&`. At 2026-09-03 ~19:00 all 8 GPUs on g3102 are IDLE (user's arms finished) while ckpt-all shows many Priority-pending jobs; the node allocation (proxy-jump) has run 2d10h. Ruling: gate-1 (2000-step) run goes DIRECTLY on this node (CUDA_VISIBLE_DEVICES=0,1,2,3, nohup, logs under slurm_logs/), the 30k run goes to the queue with --requeue as planned — cost if wrong: gate-1 competes with any new local work the user starts.
Task 7: fix round 1/5 (Critical + 6 Important + spec item + cheap fixes addressed, 0 open; commits ed86048..d49c9da). Re-reviewer probed nnx.clone independence and the 3-item Orbax round trip.
Task 7: minor (deferred): batch-level masked means copied per sample for reproj_px/uv2d_px/head_vs_reproj_px (should be sum/count accumulators at real val size); rigid_spread keys omitted (not nan) when no 3D; no test for the new evaluate outputs; duplicate save at total_steps % save_every == 0; host-side per-sample match loop runs twice per window
Task 7: complete (commits 6156619..d49c9da, review clean after 1 fix round; minors deferred)
Task 8: dispatched (BASE d49c9da, implementer sonnet). Scope ruling: gate-1 run (2000 steps) local on idle GPUs 0-3 and judged by reading the PNGs; 30k run queued (job id recorded); Step 4's baseline script written and dry-run on the gate-1 checkpoint; the 30k comparison is a follow-up once the queued job finishes.
Task 8: gate-1 run OOMs at batch 16 (4/GPU): a single op requests 35.14 GiB on 3 of 4 GPUs, then the NCCL clique rendezvous hangs (PID 4159979 alive, 0% util). Controller instructed the implementer to kill it and bisect the allocation on one GPU at batch 1; queued 30k job 39552588 (Priority pending) must be cancelled until the fix lands.
Task 8: bisect (implementer, 1 GPU): culprit = chunked masked_attention under grad — lax.map retains every chunk's logits/softmax in reverse mode (8.5 GB vs 3.5 GB unchunked for the 4-layer 2D stack at bs4); full model +grad bs4 = 40 GB. Ruling: apply  (measured 1.9 GB) as a separate commit + a finite-gradient test through the chunked path; relaunch gate-1 at the shipped batch 32; re-submit 30k — cost if wrong: extra recompute per chunk (~15% decoder time).
Task 8: (correction of the previous line, whose backticked code was eaten by the shell) Ruling: apply `jax.lax.map(jax.remat(attend), qh)` in masked_attention (measured 1.9 GB vs 8.5 GB) as a separate commit plus a finite-gradient test through the chunked path; relaunch gate-1 at batch 32; re-submit the 30k job.
Side task (user go-ahead 2026-09-03 ~19:45): measurement-only profiling of the mvq train step on idle GPUs 4-7 (scratchpad scripts, no repo edits) while gate-1 trains on 0-3; speedup implementation becomes a reviewed task after Task 8. Baseline measured: 3.0 s/step at global batch 32, GPUs 100%, trainer 44% CPU.
Task 8: 30k job 39553101 started RUNNING on g3126 at ~19:50 (code at e11cb59). Gate-1 at step 400 (loss 1567, from 1767 at step 50).
Task 8: gate-1 (batch 32, 4 GPUs) step-1000 eval — train match_reproj 17.0 px; val prompted mpjpe 10.53 units (1.05 mm) reproj 72.3 px exist P/R 0.73/1.0; val unprompted 9.45 units (0.95 mm) reproj 71.7 px exist P/R 0.56/1.0. Then the step-1000 Orbax save (async D2H of 1217 arrays) coincided with the next step's allocation -> BFC OOM on one device -> NCCL rendezvous hang. Implementer relaunched with train.save_every=999999 (measurement-only run). REAL FIX NEEDED in train_mvq._save_step: wait_until_finished() after save (synchronous) so device memory settles; then 30k resubmit with save_every=250 because ckpt-all preempted job 39553101 twice (8 min, 26 min) before its first save.
Profiling (GPU 4, batch 8, fp32/TF32): baseline 2.78 s, peak 26.9 GB; backbone fwd+bwd 1.17 s (42%); refine pass 0.63 s (23%); q_chunk=512 chunking+remat 0.37 s (13%); global fusion 0.07 s; remat=False OOMs even at batch 4; cuDNN flash rejects seq_len 789 (needs padding to a multiple of 64 — untested); bf16 backbone cast only 1.11x (fp32 already runs TF32 tensor cores). Realistic non-modeling wins: q_chunk=None (-13%, +2.2 GB), synchronous save; modeling levers (refine pass, 2D-path depth) are P3 ablations.
Task 8: CORRECTION — the post-step-1000 OOM recurs with checkpointing disabled, so the trigger is evaluate() (unsharded on GPU 0, separate _fwd executable), not the Orbax save: the next train step cannot get its ~17.2 GiB contiguous XLA temp arena (cost_analysis temp_size 19.3 GB) on the fragmented GPU_0 pool -> BFC OOM -> NCCL hang. Ruling: (a) gate-1 measurement run relaunched with eval only at the end (train.eval_every=2001) so the figures land; (b) code fix as a separate reviewed commit: shard eval batches over the mesh (same layout as training), `del em` + `jax.clear_caches()` after each mid-run eval, and set TF_GPU_ALLOCATOR=cuda_malloc_async in the launch env (the allocator's own recommendation) — validated on GPUs 4-7 with a 300-step run at eval_every=100 before the 30k resubmit; (c) 30k resubmit with save_every=250 after (b). The earlier "synchronous save" ruling is withdrawn as the primary fix (keep wait_until_finished only if it costs nothing).
Task 8: gate-1 final eval (2000 steps, batch 32): prompted 0.47 mm / 32.7 px, exist P/R 1.00/0.60; unprompted 0.38 mm / 26.7 px, P/R 0.85/0.85; rigid spread 0.03-0.05 mm. Per-sample (unprompted): female 0.40 mm / 24 px (n=67), two-fly 26 px (n=74), calib group A 0.35 mm / 22 px (n=123), group C (val-only calibration) 0.48 mm / 29 px (n=30) -> ROI-local + geometry tokens generalise across calibration groups.
Task 8: controller READ figures/2026-09-mvq/mvq_t1_b16_gate1/unprompted/gate1_female.png and gate1_two_fly.png: green (reprojected 3D) and cyan (2D head) sit ON the fly in all 7 cameras (cam1..cam7) with no constant offset and no mirrored/rotated camera; leg chains follow the legs; tarsal tips under-reach the white labels (the residual 17-23 px is at the tips). Two-fly worst row (#10, female, group C, 0.83 mm, 40-59 px) is a displaced prediction, not a swap onto the other fly. GATE 1: PASS (geometry bookkeeping verified).
Task 8: controller READ gate1_worst.png (unprompted): the six worst (0.75-0.87 mm, 40-60 px) are two males in close contact (rows #65/#67, group A: the other fly directly adjacent in cam1/cam3, predictions stay on the correct fly) and four group-C females with legs fully extended (wall/edge poses): predictions hug the body and under-reach the leg tips (mean-pose regression at 2k steps), with the 2D head (cyan) and reprojected 3D (green) disagreeing most in cam3 for row #10 (crop clamped at the image edge). No identity swaps, no geometry errors. Failure mode = the spec's "regression precision gap" risk on extended legs; watch it in the 30k run and the refinement-pass ablation.
Task 8: implementer DONE (e11cb59, 7aa34ff, bcbc801). Gate 1 PASS. Baseline ViTPose+DLT on the same 153 val framesets: 0.107 mm overall / 0.072 female / 0.170 two-fly vs mvq@2k 0.38 mm unprompted. 30k job 39557158 requeued 3x, passed 2 saves. Review dispatched (opus).
Task 8: review (opus) — 9 Important (baseline joint-set fairness, coverage fields, hardcoded detector ckpt/root, uncaveated detector exposure to v12 val, rigid spread-only, eval batch 8->32, ragged-pad untested, cam{c+1} labels, s/step trigger). Rulings: same-joint-set scoring + coverage; rigid_len_ratio metric; count-weighted px metrics with eval batch kept; camera serials via V12WindowDataset.camera_names; notes cite the profiler for the loader. Folded in: EMA bias correction (controller-diagnosed: 0.999^2000=0.135 of the init head => 0.86-0.93 radial ratio on body groups) and q_chunk as an MVQConfig field defaulting to None (profiler -13%). Fix round 1/5 dispatched (resume implementer). 30k run moved LOCAL on 8 GPUs (mvq_t1_b16_local8_20260904, PID 642747); queue job 39557158 cancelled.
User go-ahead 2026-09-04 00:15: implement the cuDNN flash-attention path (backbone no-mask bf16; decoder with even-padded key mask; attn_impl switch with explicit fallback; GPU parity test) as an added task (Task 9) once the queued microbenchmark (job 39561699) confirms a gain; gate adoption on a short 4-GPU A/B (queue, or pause the local 30k at a checkpoint boundary) showing wall-clock gain with val within noise. Task 9 dispatches after the Task 8 fix round lands (one implementer at a time).
Flash benchmark (job 39561699, 1 GPU, N=56 x 12 heads x 789 x 64, fwd+bwd): explicit fp32 26.6 ms; explicit bf16 14.8 ms; cudnn bf16 T=789 FAILS even without a mask (even-length rule applies under grad regardless); cudnn bf16 T=790 no-mask 4.7 ms (5.6x vs fp32); cudnn with key mask T=790/832 9.7/9.6 ms (2.7x). Confirmed -> Task 9 (flash attention) is a go. Local 30k: 1.75 s/step on 8 GPUs (4/GPU) => ~14.6 h.
Task 8: fix round 1/5 — commits f64c49a (code) + 57b285c (scripts/notes); 22 passed; rigid_len_ratio 0.43-0.75 confirms shrink; baseline coverage 1.000 (fairness moot on this val). Scoped re-review dispatched (opus).
Task 8: fix round 1/5 (10 addressed, 2 open — A2 zero_mask_channel not read/asserted; NEW Important: debiasing a pre-fix params-seeded EMA on resume silently scales params by 1/(1-0.999^t) — the live 30k run's checkpoints are exactly that, so the planned "resume with new code" is UNSAFE; commits bcbc801..57b285c)
Task 8: Ruling: the local 30k run will be RESTARTED fresh (not resumed) once round 2 + Task 9 land; round 2 adds a persisted EMA update counter and refuses checkpoints without it.
Task 8: fix round 2/5 dispatched (resume implementer)
Ruling (supersedes "restart the 30k"): KEEP the local 30k run (mvq_t1_b16_local8_20260904) to completion — its final EMA at 30k is unbiased to ~1e-13 and q_chunk/flash change speed not math, so it is a valid P2 artifact (~13 h remaining); only its mid-run evals before ~step 5000 carry EMA bias. Task 9's A/B goes to the queue. Cost if wrong: ~4-5 h of GPU time that a faster relaunch might have saved.
Task 8: fix round 2/5 — commits 9eeb140 (ema_meta item + refusal guard, pad-row-excluded px weights, _finish NaN guard) + ab489c1 (baseline zero_mask_channel read/applied, ValueError on length mismatch, per-row C, notes); 23 passed. Scoped re-review dispatched (sonnet). Task 9 (flash attention) implementer dispatched concurrently (BASE ab489c1, sonnet) — different files; if round 3 is needed it waits for Task 9.
30k local run: step-2000 eval (prompted 0.54 mm / unprompted 0.40 mm, EMA-biased at this step) completed and training continued past step 2050 on 8 GPUs — the sharded-eval fix holds at the point that killed the two earlier runs.
Task 8: fix round 2/5 (2 addressed, 0 open; commits 57b285c..ab489c1)
Task 8: minor (deferred): mvq_val_baselines.py module docstring still describes the all-zero 4th channel unconditionally
Task 8: complete (commits d49c9da..ab489c1, review clean after 2 fix rounds; minors deferred)
Task 9: implementer DONE (4f1802c code+tests, f571d8f notes). GPU parity 3/3 (second job; first caught a test-shape bug). A/B (job 39563250, 4x L40S, 300 steps): xla 2.66 s/step vs cudnn 1.54 s/step (1.73x), val deltas <1% at 150/300 -> ADOPT (mvq.yaml attn_impl: cudnn). Caveat: backbone pads to even WITHOUT a mask (~0.13% attention mass to a zero key) — same path at train and inference. Review dispatched (opus).
Task 9: review (opus) — Critical: backbone pads to even WITHOUT a mask (unforced ~0.06-0.13% attention attenuation x12 pretrained layers) on a premise the installed jax source contradicts (even-length check at fused_attention_stablehlo.py:392 is gated on has_bias); the one sensitive odd-N test was reshaped to even-N. Important: "never materialises logits" false for the masked decoder branch (bias tensor + dbias); notes' exist_prec/rec "track closely" claim false at step 300; commit note's "moves no numbers" framing false. Minor: no attn_impl validation; dead qv & ; helper layering.
Task 9: Ruling: resolve EMPIRICALLY on the queue — (1) if (2,789,12,64) bf16 mask=None under grad runs, pad only when a mask is passed (backbone exact); (2) else use key_value_seq_lengths/query_seq_lengths (MaskType.PADDING, no bias) to exclude the pad exactly; (3) only if both fail keep the approximation with a tight odd-N test. Fix round 1/5 dispatched (resume implementer).
Task 9: fix round 1/5 — commits 747d90b (code) + 317fd38 (docs). Empirical (job 39568764, bf16 (2,789,12,64) under grad): (a) unpadded no-mask FAILS (the reviewer's source reading was wrong; the benchmark was right), (b) 790-padded + key_value_seq_lengths=789 runs EXACT at 7.94 ms, (c) bool mask 18.02 ms. Backbone now uses (b). Odd-N=17 parity test restored at 2e-3. Scoped re-review dispatched (opus).
Task 9: fix round 1/5 (Critical + 2 Important + minors addressed, 0 open; commits f571d8f..317fd38). Re-reviewer explained the (a) failure: fused_attention_stablehlo.py replaces bias with a zeros(0) sentinel before check_is_flash_attention, so has_bias is always True under training — the even-length rule is effectively unconditional; the benchmark was right.
Task 9: minor (deferred): attention.py's last docstring edit postdates the GPU parity job by ~2 min (logic unchanged; one re-run when a GPU frees); test_mvq_model full-model bound loose for uv (~1.3 px on a 64-px crop); gpu-marked tests not auto-skipped on CPU hosts; dinov3.py attn_impl field comment stale about the pad
Task 9: complete (commits ab489c1..317fd38, review clean after 1 fix round)
FINAL REVIEW: dispatched (fable) over ea087c0..317fd38
FINAL REVIEW (fable): Ready with fixes. Critical: none. Important: (1) camera-dropout floor keyed on frame 0 (T=2-unsafe); (2) reported MPJPE picks an oracle instance (argmin to GT), not the shipped policy — prompted numbers are not prompted-instance numbers; (3) backbone=dinov3_l16 silent no-op; (4) plan dropped spec §6.3 gray-fill/cutout without a ruling and spec §8's "crop exactly as build_frameset" (which gray-fills) contradicts the set-predictor training; (5) assemble/crop_origin seam untested (loader drops origin; assemble takes (B,C,2) vs spec (T,C,2)). Minors incl. px_scale not recomputed after per-view affine scale, FusionStack ignores q_chunk, duplicated loader + no ckpt/ (debias) loader, has_mask before dropout, stale docstrings.
Ruling on (4): P4 does NOT gray-fill — the model is trained as a set predictor with the other fly visible plus repulsion; the spec §8 sentence is amended to "crops centred as build_frameset centres them, WITHOUT the distractor gray-fill"; training gray-fill stays available only as a future P3 augmentation flag if two-fly val regresses. Cost if wrong: two-fly frames see a distribution the model never trained on — the P3 two-fly cohort A/B will show it.
FINAL FIX WAVE: one dispatch (sonnet) — Important 1,2,3,5 + px_scale recompute + FusionStack q_chunk + has_mask after dropout + composed-aug test + shared checkpoint loader (final/ and ckpt/ with debias, attn_impl override) + stale docstrings + spec/notes amendments (ruling 4, §4.1 crop_origin (B,C,2), uv in crop px, conf lambda, recorded deviations). Deferred to P3/P4 per the reviewer's triage: everything else.
FINAL FIX WAVE: commits 163225e (lib+tests), 95b628f (scripts), ff58a7c (docs); 69 passed. Scoped re-review dispatched (opus).
FINAL FIX WAVE re-review (opus): all 12 items addressed, no new Critical/Important. 69 passed / 4 gpu-deselected.
Parked with rulings (no second fix wave, per process): (1) evaluate's prompted policy ignores per-sample has_mask (mask-less windows scored as prompted) — real, small; P3 fixes before the prompted/unprompted ablation; (2) ckpt/<step> loader requires final/mvq_run.json so the "before final save" case is unreachable — real, P3 (write mvq_run.json at run start); (3) mvq_meta_val stale under --mvq_step — real, P3; (4) >= vs > 0.5 prose/code; np.mean([]) nit; policy fields lack _mm twins; backbone="tiny"+pretrained KeyError; spec §6.3 dropped the word "cutout" (record as not-implemented in P3 plan); spec §10 says 12 PNGs (6 exist). Ruling: none load-bearing for merge; all listed for the P3 plan.
BRANCH COMPLETE: ea087c0..ff58a7c (27 commits), final review clean after one fix wave. 30k local run continues (step ~8500, prompted 0.120 mm / unprompted 0.115 mm oracle at step 8000 vs DLT baseline 0.107 mm).
