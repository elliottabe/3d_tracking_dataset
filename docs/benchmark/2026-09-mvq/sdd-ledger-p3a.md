# SDD ledger — plan: docs/plans/2026-09-04-mvq-p3a-identity-existence.md

Spec: docs/specs/2026-09-04-mvq-p3a-identity-existence-design.md (read; binding authority).
Worktree: .claude/worktrees/mvq-p3a, branch mvq-p3a from 3155759 (elliottabe/paper_update_062026 HEAD).
Baseline: mvq CPU suite last green at merge 809148f (127 passed); re-run started at setup (background).

## Pre-flight scan (2026-09-04)

| pair / task | produces vs consumes | finding | ruling |
|---|---|---|---|
| T1 -> T3 | `assign_slots(fly_sex, fly_valid, prompt_on, dist, n_instances)`, `slot_ignore`, `SEX_*`, `N_SLOTS` | names identical in both tasks | clean |
| T1 -> T2 | T2 imports `SEX_FEMALE/SEX_MALE/SEX_UNKNOWN/SEX_PRESENT_UNKNOWN` from matching.py | defined in T1 | clean |
| T2 -> T3 | batch keys `fly_sex (B,F) int8`, `unlabelled_sex (B,) int8` via WINDOW_KEYS | T3's `_perfect_batch` builds the same dtypes | clean |
| T2 -> T5/T6 | sample keys used by `composite`: crops, prompt_mask, kp2d, vis2d, kp3d_local, has3d, fly_valid, fly_sex, unlabelled_sex, cam_valid, M, t_local | all present after T2 | clean |
| T4 -> T3 | `out["sex_logit"] (B,I)` | T3 fixture supplies it; T4 model emits it | clean |
| T4 -> T7/T8 | `n_instances=4` tiny configs | T7/T8 tests use 4 | clean |
| T5 -> T6 | `CopyPasteParams` fields p/opposite_sex_p/contact_p/contact_sep/far_sep/max_tries/gain_clip; `composite -> dict|None` | T6 uses the same | clean |
| T6 -> T7 | `V12WindowDataset(copy_paste=...)` | T7 constructs `CopyPasteParams(p, opposite_sex_p, contact_p)` | clean |
| T7 -> T10 | `MVQTrainConfig.warm_start`, `copy_paste_p` hydra overrides | T10 launch line uses `train.warm_start`, `train.copy_paste_p` | clean |
| T8 -> T9 | `ds.fly_centroids` (T2) for contact_pair in overlay + `_cohorts` | same 15-unit rule stated in both | clean |
| T1 self | test `same_sex_pair_nearer_fly_takes_typed_slot` asserts host-first beats distance; code processes host first | consistent | clean |
| T3 self | `test_loss_near_zero...` bound `total < 0.05` with the new sex term ~0.0012 | consistent | clean |
| T5 self | fixture masks: plan flags that `make_v12_root` may not write masks | judgement call left to implementer, flagged in brief | note |
| T7 self | flax `nnx.to_pure_dict`/`replace_by_pure_dict` availability | flagged in brief | note |
| T4 self | mv_augment pass-through of new int8 keys | flagged in brief | note |
| Global vs T1 | `mvq_loss` raises unless I==4 while `assign_slots` accepts any n_instances | intended: the function is general, the loss pins the spec's 4 | clean |

Rubric check: no test asserts nothing; no verbatim duplicated logic block mandated (T8's centroid code repeats the loss's 3-line centroid — acceptable, numpy vs jax).

## Progress
Baseline (worktree, CPU, -m 'not gpu', files untouched by T1): 50 passed, 4 deselected. test_dinov3.py::test_attn_impl_cudnn_matches_xla_backbone needs the gpu marker deselected (pre-existing). BASE for Task 1 = 3155759.
Task 1: review 1 — spec ✅ except plan-mandated Important: two valid unknown-sex flies both land on slot 3 (collision hidden by bool slot_target). Minor (deferred): no F<=2 runtime guard; docstring names `slot_target` vs internal `taken`.
Task 1: Ruling: a fly whose resolved slot is already taken (only reachable for a 2nd unknown-sex fly, since typed-slot overflow already falls to slot 3) gets assign = -1 and no target — it is treated like an unlabelled fly rather than corrupting a typed slot — because the spec (§3) is silent on two unknowns and the v12 export has no unknown-sex fly at all; cost if wrong: a future export with unknown-sex pairs silently drops the second fly's geometric loss (visible in fly_valid/assign stats, not silent in the ledger).
Task 1: fix round 1/5 (1 addressed, 0 open — slot-3 collision -> drop to -1; commits 30c8aaf..43b0b32)
Task 1: complete (commits 3155759..43b0b32, review clean). BASE for Task 2 = 43b0b32.
Task 2: complete (commits 43b0b32..6d8370e, review clean). Minor (deferred): unused `rec_` in __getitem__; fly_centroids docstring caveat for T>1 (NaN row when a window-labelled fly lacks frame-0 labels). BASE for Task 3 = 6d8370e.
Task 3: review 1 — Important (plan-mandated): a VALID fly dropped by assign_slots (assign=-1, e.g. 2nd unknown-sex fly) is clamped to slot 0 by _gather_inst and its labels score against slot 0 (terms 1-5, 7, aux) because masks use fly_valid alone. Minor (deferred): redundant `& fv` in the sex one-hot.
Task 3: Ruling: define `fv_eff = fv & (assign >= 0)` right after assignment and use it in place of `fv` for every per-fly gathered term (terms 1-5, repulsion both roles, aux layers, and the metrics mp/px/uv2d_px/head_vs_reproj_px); existence/sex targets are unaffected (they are slot-axis). A dropped fly's labels then contribute to no term at all (same as an unlabelled fly) — consistent with the Task 1 ruling; cost if wrong: repulsion loses the dropped fly's labels as a repeller (negligible; unreachable on the current export).
Task 3: fix round 1/5 (1 addressed, 0 open — fv_eff masks dropped flies from every geometric term; commits 813ec9b..fabbb1c)
Task 3: complete (commits 6d8370e..fabbb1c, review clean). BASE for Task 4 = fabbb1c.
Task 4: complete (commits fabbb1c..ca16464, review clean). BASE for Task 5 = ca16464.
Task 5: implementer reported two deviations from the brief's code: (1) view_shifts sign: shift = M D + t_tgt - t_src (the brief had src/tgt swapped; the brief's own exactness test is the authority) — Ruling: accept, the plan's formula was wrong, the spec's exactness requirement is what binds; cost if wrong: none (test-pinned). (2) rejection criterion switched from pasted LABELS leaving the crop to pasted MASK leaving the crop, out-of-crop keypoints marked invisible — pending the Task 5 review (opus) before ruling.
Task 5: review 1 (opus) — spec met incl. controller additions; deviation #1 (shift sign) verified correct. Important: (1) off-crop pasted keypoints keep has3d/3D target with no pixels in any view; (2) camera skipped for an empty donor mask still marks donor keypoints visible there (plan-mandated); (3) fly_valid/fly_sex arrays replaced (hardcodes F=2), no precondition guard on tgt fly_valid[1]/unlabelled_sex; (4) occlusion / prompt-mask subtraction / gain paths have zero effective coverage (test passes by equality at D=(12,0,0)); no real-frame figure. Minors: exact-pixel test non-vacuous only by seed; fixture npz uncompressed (24 MB/call); full-frame medians per camera; all-or-nothing mask rejection; unused params in this module.
Task 5: Ruling: deviation #2 (mask-based rejection) ACCEPTED provided the label/pixel invariant is restored by fixing (1) and (2) — labels never survive without pixels; cost if wrong: a paste whose mask is mostly off-crop is accepted with in-crop labels (the review's coverage-floor minor, deferred to the final review list).
Task 5: Ruling: the real-frame copy-paste figure is Task 9's gate (scripts/viz/mvq_copy_paste_check.py, read before launch), not a Task 5 deliverable — plan structure; cost if wrong: a compositing defect only visible on real masks surfaces one task later, before any training.
Task 5: minor (deferred): all-or-nothing mask rejection (coverage floor); unused p/opposite_sex_p/max_tries in this module (consumed by Task 6).
Task 5: fix round 1/5 (4 addressed, 0 open — dead-label rule, skipped-camera vis, in-place fly arrays + precondition guard, occlusion/prompt/gain tests; fixture gains manifest_n_flies knob; commits a8054d6..8086032)
Task 5: complete (commits ca16464..8086032, review clean). Minor (deferred): `cleared.sum() == 14` magic pin in the D=0 test; `painted` gates on the donor's own pre-shift mask so a donor with masks in only some cameras yields a partially-visible fly 1 (confirm intentional when real masks flow). BASE for Task 6 = 8086032.
Task 6: review 1 — approved; Important (plan-mandated): paste_window returns None on the first empty pool draw instead of exhausting max_tries (same-sex draw with an empty own-sex pool never falls back to the opposite pool). Minor (deferred): double _build(i) on a failed paste.
Task 6: Ruling: fix it (cheap) — resolve the wanted pool, fall back to the other sex pool, and return None only when BOTH are empty after excluding i; cost if wrong: none measurable on v12 (every calib group has hundreds of donors of each sex except group B = female-only, where the fallback already handled the opposite draw).
Task 6: fix round 1/5 (1 addressed, 0 open — other-sex pool fallback + test; commits 8b277a3..4202b16)
Task 6: complete (commits 8086032..4202b16, review clean). BASE for Task 7 = 4202b16.
Task 7: implementer DONE with a correctness concern: warm_start_partial hardcodes decoder/heads/sex/{kernel,bias} as always-fresh because the plan's test saved a 3-slot source built with the NEW model (which already has a sex head of matching shape). Ruling: the hardcode is a test artefact, not the spec — the source must emulate the real 30k checkpoint (no sex head): the test drops the sex leaves from the saved source state, and warm_start_partial skips leaves only by path-miss or shape mismatch (plus the e_inst row rule); cost if wrong: none for the P3a launch (the 30k final/ has no sex head either way). Pre-review fix dispatched.
Task 7: pre-review fix (sex-head hardcode removed; commits ce6f300..96d6470). Review (opus): approved, 0 Important. Minors (deferred unless noted): (1) print restored/total leaf count on warm start [folded into Task 8 dispatch, train_mvq.py]; (2) annotate shape-mismatch skips; (3) production case (state-form source, sex subtree dropped, 3->4) not tested as one case; (4) warm_start smoke test asserts no weights; (5) final/ fallback branch untested; (6) endswith("e_inst"); (7) in-place mutation not documented; (8) non-array leaves reported skipped; (9) orbax metadata accessor version-sensitive; (10) test GREEN run used -W ignore::DeprecationWarning (plan's .value writes); (11) continuation indents.
Task 7: complete (commits 4202b16..96d6470, review clean). BASE for Task 8 = 96d6470.
Task 8: review (opus) approved, 0 Important. Minors (deferred): legacy exist_prec/rec print 0.0 instead of nan on zero denominator; mask_containment polarity note (losses.mask_containment is the inverse); dead per_sample fields p[2],p[3],p[7] (n_flies scan per sample); loop-local import with unused SLOT_OTHER; per-sample np.asarray(has_mask)/device project_local; duplicated assignment math vs loss; `c` shadowing in _contact; duplicate test assertion; no unit test pinning containment orientation; indent nit.
Task 8: complete (commits 96d6470..c46713d, review clean; full mvq CPU suite 96 passed / 4 gpu deselected). BASE for Task 9 = c46713d.
Task 9: gates read by the controller (Read tool): sex-label check — no reversal in any of the 4 mixed recordings (17_28_34 and 15_25_51 strong; 20_04 weak-consistent; 12_11_50 inconclusive, fly1 rarely in crop). Copy-paste check — donors consistent across all 7 cameras, occluded host keypoints hollow, far rows correctly blank where the shifted mask leaves the crop.
Task 9: Ruling: "headless" donors are NOT a data-QC defect — the manifest lists four recordings with behavior=headless (a real experimental condition), so a headless donor is a legitimate fly appearance; no head filter in the production loader (the check script's filter stays for legibility only); cost if wrong: some pasted pairs show a headless second fly, which real headless recordings also do.
Task 9: Ruling: the contact_pair cohort threshold moves from 15 to 30 world units (3 mm) — the real stacked mating pairs (#60-#67, the failure cases this cohort exists for) have centroid separations >= 24 units, so 15 selects nothing on real data; CopyPasteParams.contact_sep widens to (8, 30) so the augmentation covers the real mounting regime as well as the heavier-overlap cases; the overlay's _is_contact gets its own 30-unit constant (decoupled from copy-paste); spec §6/§7 amended accordingly; cost if wrong: the cohort is slightly broader than "in contact" (a 3 mm centroid gap can be two flies abutting, still the intended hard case).
Task 9: fix round 1/5 (2 rulings applied — headless reword; CONTACT_UNITS=30, contact_sep (8,30), spec §6/§7 amended; val census at 30u: 26/153 windows, all 2026_04_02_15_25_51 ids 42-67 incl. #60-#67; commits 1ee6ceb..94ae550). Review dispatched (sonnet).
Task 9 note for P3b: copy-paste limitations raised by the user 2026-09-04 — donor always on top in every view (no per-camera depth ordering; affine cameras need a per-rig viewing-direction sign), and contact offsets lie in the body plane so close pairs interpenetrate instead of stacking (add a normal-direction offset, sign away from the floor). Both recorded as P3b items.
Task 9: review (sonnet) approved, 0 Important; trailers on both commits verified by the controller (4/4). Minors (deferred): same-sex backfill branch in the check script untested; eager build of all mixed-recording windows; no regression test for CONTACT_UNITS.
Task 9: complete (commits c46713d..94ae550, review clean).
Task 10: Ruling: the P3a job launches from the MAIN checkout after the final review and merge, not from this worktree — a SLURM requeue re-imports code from the job's cwd, and this morning's worktree-cwd run showed why a long job must not depend on a worktree we intend to remove; cost: the launch shifts by the length of the final review (~1 h), during which the 30k final/ (needed for the warm start) lands anyway.
30k run FINISHED 12:31 (final/ written); node g3102 GPUs now idle. Task 10: Ruling: launch the P3a fine-tune LOCALLY on the 8 idle GPUs after merge (the user's ckpt-all choice was made while the node was busy with the 30k run; the compute-node memory rule says run directly on an idle node rather than sbatch-and-wait); the ckpt-all submit line stays documented as the alternative; cost if wrong: the node allocation (2+ days left) is consumed by a ~4 h job instead of the queue.
FINAL REVIEW (opus): ready WITH FIXES. Critical: C1 sex label of 2025_10_20_13_20_04 (26% of train) resolved by dict order from a 15-frameset minority block; C2 load_mvq_model raises on every pre-P3a checkpoint (608 vs 606 leaves) so the 30k baseline/acceptance is uncomputable. Important: I1 slot_ignore ignores slots a labelled fly occupies; I2 acceptance metrics not per cohort (sex_acc/mask_containment/policy_miss_frac); I3 overlay+baseline scripts carry a stale unprompted policy (slot 0 included); I4 overlay draws only the oracle slot; I5 phantom fly 1 when the donor mask is empty in every camera; I6 spec §6 not amended for mask-based rejection / donor fallback. Minors + deferred triage recorded in the review output.
C1 census (controller): rec 2025_10_20_13_20_04 fly0 = 677 framesets subset `courtship_20_04_male` (annotation sex male, frames 84143-439478) + 15 framesets subset `20_04_female_climbing` (female, frames 446642-447638). Every other (rec, fly) is single-subset and self-consistent. The annotation sex agrees with the subset names; the manifest's fly0=female came from parsing the concatenated dirname.
Ruling C1: sex is resolved PER WINDOW from the host frameset's own annotation (`_frameset_own_sex`), manifest per-fly sex only as fallback when the annotation lacks it; `is_female(i)` (sampler + cohorts) uses the same per-window value; the unlabelled animal's sex in a recording with manifest sex "mixed" and n_flies 2 is the OPPOSITE of the host's own sex (the two subsets label different animals); loader warns once per (rec, fly) whose framesets disagree. Consequence: 677 train windows previously counted as female-host are male-host (the earlier "train female-host 0.33" census and the balanced sampler were wrong for them). Cost if wrong: none — the per-annotation sex is the annotator's own label and matches the subset name; the 15 climbing-female windows keep female.
Ruling C2: `load_mvq_model` becomes shape/path tolerant (same merge as warm_start_partial, returns/prints the unrestored leaves) so 3-slot checkpoints load; `mvq_loss` keeps its I==4 guard (training/evaluate need 4 slots); the 30k BASELINE for mask_containment/policy is produced by the overlay script's per-sample summary (it gets mask_containment), not by evaluate. Cost if wrong: evaluate() cannot score legacy models — acceptable, the scripts can.
Ruling I3/I4: one shared `policy_instance(...)` in models/mvq/policy.py used by evaluate, mvq_overlay and mvq_val_baselines (typed_only when I==4, legacy any-slot rule when I==3); the overlay draws every existing typed slot colour-coded (gate-3 needs it).
Fix wave dispatched (opus) with the complete list; one scoped re-review follows.
C1 RULING REVERSED (user, 2026-09-04, from the regenerated gate figure): all three 20_04 rows — including frame 84143 from subset courtship_20_04_male — show a FEMALE; the reviewer's own size measurement (the "male"-annotated block is ~15% LARGER) agrees. The annotation `sex` field of subset courtship_20_04_male is wrong; the manifest fly0=female is right. New rule: manifest per-fly sex first, annotation fallback, disagreement warning kept; unlabelled_sex back to manifest-id logic. Consequence: no window changes sex; the P2 census and sampler were correct. P3b: fix the export's annotation sex for that subset. Cost if wrong: none beyond what the annotation field already encoded; the user's visual call is the authority for this recording.
C1 FINAL (user verdict on the full-frame render, 2026-09-04 ~14:15): in 2025_10_20_13_20_04 the labelled fly is the MALE in the courtship_20_04_male block (677 framesets) and the FEMALE in the 20_04_female_climbing block (15 framesets) — the two annotation subsets labelled different animals under the same fly id. Ruling: ANNOTATION-FIRST per-window sex resolution (frameset's own annotation sex -> manifest per-fly -> recording sex -> unknown), the unlabelled animal in a manifest-"mixed" two-fly recording is the OPPOSITE of the host's own sex, the disagreement warning stays; female-host census becomes 202/2661 (the P2 balanced sampler over-weighted 677 male windows as female). Cost if wrong: none beyond the user's own visual call; both size invariants agree for the 677.
Final fix wave (commits 43e17d5..44bb882): scoped re-review (opus) — ALL findings addressed, no new Critical/Important breakage; verified by running the loader/warm-start/loss/loader tests. Minor (deferred, P3b): merge_state_by_path silent on checkpoint-only leaves; tolerance unconditional (baselines script prints a partially-restored model); ckpt/ branch tolerance untested against a mismatched arch; overlay legend may name a sub-threshold oracle slot; sex_label_check reaches into ds._fs; smoke suite slower (~6 min).
Pending: C1 annotation-first flip commit (in progress) + spec §1 fact correction; then one scoped re-review of that commit, then finishing-a-development-branch (merge to elliottabe/paper_update_062026) and the LOCAL 8-GPU launch from the main checkout.
C1 flip commit e37502d: scoped re-review (sonnet) — addressed, no new breakage (edge cases (a)/(b) verified by execution). Branch head e37502d = 22 commits over 3155759. User chose: merge locally to elliottabe/paper_update_062026, then launch on the idle 8 GPUs from the main checkout. Full-suite run on the branch head in progress.
Full suite on branch head e37502d: 102 passed, 4 gpu-deselected (350 s).
